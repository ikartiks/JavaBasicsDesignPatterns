class Coffee2 {
	enum CoffeeSize {BIG, HUGE, OVERWHELMING }//enum datatype{[possible values]}
	CoffeeSize size;
	//int size;
}
public class CoffeeTest2 {
	
	
	public static void main(String[] args) {
		Coffee2 drink = new Coffee2();
		drink.size = Coffee2.CoffeeSize.BIG; // enclosing class
		// name required
		//enum Traffic { RED, YELLOW, GREEN };	The member enum Traffic can only be defined inside a top-level class or interface
		Boolean []ba[];
		}
}